name: Validate Notebook

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install nbformat nbclient

      - name: Validate notebook structure
        run: |
          python -c "
          import nbformat
          nb = nbformat.read('PySpark_NYC_Taxi_Lab.ipynb', as_version=4)
          nbformat.validate(nb)
          print(f'Notebook is valid. {len(nb.cells)} cells.')
          "
        # Note: PySpark_NYC_Taxi_Lab.py is a flat reference copy of the notebook's
        # code cells and includes Colab shell-magic lines (e.g. `!pip install`),
        # so it isn't run through a standard Python compile check here.
